# 怎么把这份源码变成能装的 IPA

## 先说清楚：这台电脑出不了 IPA

iOS 的 App 只能由 Apple 的 `xcodebuild` 打包，而它**只跑在 macOS 上**。刚在这台机器上查过：

| 需要的东西 | 现状 |
|---|---|
| Xcode / xcodebuild | 没有（Windows 根本没有这个工具） |
| git | 没装（正在装） |
| gh (GitHub CLI) | 没装 |
| WSL | 有 Ubuntu —— 但 **Linux 也跑不了 Xcode**，这条路是死的 |
| Docker | 装不了 macOS 容器（违反 Apple 许可，镜像也不存在） |

所以 IPA 必须由一台 **Mac** 生成。下面三条路，选一条。

---

## 路线 A：浏览器 + GitHub 云端 Mac（推荐，不用装任何东西，免费）

GitHub 给每个仓库免费的 macOS 编译机。你的源码里已经写好了编译脚本，推上去它自己就出 IPA。

### A1. 建仓库

1. 打开 <https://github.com>，注册或登录
2. 右上角 **+** → **New repository**
3. Repository name 填 `jizi-ios`
4. 选 **Public**（Private 也行：免费额度 2000 分钟/月，macOS 机器按 10 倍计费，一次编译约 4 分钟算 40 分钟，够跑 50 次）
5. **不要**勾 Add README / .gitignore / license
6. **Create repository**

### A2. 把源码传上去

建好后页面上会有一行 `uploading an existing file`，点它。

**关键一步：Windows 默认把 `.github` 这个文件夹藏起来了。** 先在文件资源管理器里打开：

> 查看 → 显示 → 隐藏的项目 ✅

然后打开 `C:\Users\XIAOCAI\Documents`，把 **`jizi-ios` 整个文件夹**拖进浏览器那个上传框里。等文件列表出来（应该是 19 个文件），下面 Commit changes 点绿按钮。

**如果拖完发现少了 `.github/workflows/build-ipa.yml`**（上传框里不显示隐藏文件时就会漏），补一个：

1. 回仓库主页 → **Add file** → **Create new file**
2. 文件名那里直接输入：

   ```
   .github/workflows/build-ipa.yml
   ```

   打 `/` 的时候 GitHub 会自动帮你建目录
3. 打开 `C:\Users\XIAOCAI\Documents\jizi-ios\.github\workflows\build-ipa.yml`，全选复制，粘到网页的编辑框里
4. 拉到底 → **Commit changes**

### A3. 等编译

传完（或 commit 完 workflow 文件）**自动开始编译**。仓库页顶部点 **Actions**，能看到一个叫 `build-ipa` 的运行在转圈。大约 **3–6 分钟**。

**如果 Actions 没自动跑**：进 Actions 页 → 左边选 `build-ipa` → 右边 **Run workflow** 按钮 → 绿色 Run workflow。

### A4. 下载 IPA

运行变绿 ✅ 之后：

1. 点进那次运行
2. 页面**最底部**有个 **Artifacts** 区域
3. 下载 `JiZi-unsigned-ipa`（是个 zip）
4. 解压，里面就是 **`JiZi.ipa`**

> 运行变红 ❌ 的话：点进去看日志，搜 `error:`，把带 error 的那几行发我，我改完你再传一次。第一次编译有报错很正常。

### A5. 装到手机

无签名 IPA 不能直接装，用自己的 Apple ID 重签（**不需要**开发者账号）：

1. 装 [Sideloadly](https://sideloadly.io/) 和 [iTunes](https://www.apple.com/itunes/download/standalone)（**必须是官网版**，微软商店版缺驱动，Sideloadly 认不到手机）
2. 数据线连 iPhone，手机上点「信任此电脑」
3. Sideloadly：**IPA** 选 `JiZi.ipa`；**Apple ID** 填自己的；Start
4. 密码那一步：如果提示要 App 专用密码，去 <https://appleid.apple.com> → 登录与安全 → App 专用密码 → 生成一个填进去
5. 手机：**设置 → 通用 → VPN与设备管理 → 开发者 App** → 信任你的 Apple ID
6. 打开「集字」

**免费 Apple ID 的限制：签名 7 天过期**，同时最多 3 个自签 App。过期后 Sideloadly 再来一遍。

嫌每周手动烦 → 装 **AltStore** 或 **SideStore**，把 IPA 丢进去，它自动续签。

---

## 路线 B：租一台云 Mac（想本地改代码、反复迭代）

按小时计费，大概 ¥5–10/小时：

- [MacinCloud](https://www.macincloud.com/) — 有按小时/按天的套餐，最省事
- [Scaleway Apple Silicon](https://www.scaleway.com/en/apple-silicon/) — 按小时，M 系列
- [MacStadium](https://www.macstadium.com/) — 偏企业，贵一点

租到之后把 `jizi-ios` 文件夹传上去（用它的网页传文件功能或 iCloud），在终端里：

```bash
cd ~/jizi-ios
brew install xcodegen        # 没装 brew 的话先去 brew.sh 装
bash scripts/build-ipa.sh
```

跑完 `JiZi.ipa` 就在 `~/jizi-ios/` 下。下载回来，走上面 A5 装。

好处：编译报错能当场看到、当场改。想改代码的话这条路值。

---

## 路线 C：让我在这台电脑上直接推 + 拉（需要你给一个 GitHub token）

如果装好了 git，并且你给我一个**只勾了 repo 权限**的 GitHub token（用完随时去 GitHub 设置里撤销），我可以从这边直接把源码推上去、等编译、把 IPA 下载到 `C:\Users\XIAOCAI\Documents\`。

但**编译那一步照样在 GitHub 的 macOS 机器上跑** —— 这台 Windows 负责推和拉，不负责编译。这是路线 A 的自动化版本，省掉你手点网页的部分。

---

## 顺带说一句

这份源码是我照 API 手写的，**没在真机/模拟器上编译验证过**（这台电脑没 Xcode）。第一次编译大概率有几个错，属于正常现象——把报错发我，几轮就能修干净。修好之后的 IPA 就是能正常用的集字 App。
